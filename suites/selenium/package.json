{
  "name": "perf-suite-selenium",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "bench": "vitest run --config vitest.config.ts"
  },
  "dependencies": {
    "selenium-webdriver": "^4.45.0"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
